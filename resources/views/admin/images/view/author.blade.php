<div class="d-flex align-items-center avatar-group my-3">
    <div class="avatar" title="{{ $row->getAdd->name }}" data-bs-toggle="tooltip" data-bs-placement="bottom">
        <img src="{{ $row->getAvatar($row->getAdd->name) }}" alt="Avatar" class="rounded-circle" />
    </div>
</div>
